[phases.install]
cmds = ['echo skip-default-install']

[phases.build]
cmds = [
  'cd ../.. && pnpm install --frozen-lockfile && pnpm --filter afters-messaging build'
]

[start]
cmd = 'node dist/main.js'

providers = ['node']

[phases.install]
cmds = [
  'npm install -g pnpm@9.12.0',
  'pnpm install --frozen-lockfile'
]

[phases.build]
cmds = [
  'pnpm --filter afters-messaging build'
]

[start]
cmd = 'cd afters-messaging && node dist/main.js'
